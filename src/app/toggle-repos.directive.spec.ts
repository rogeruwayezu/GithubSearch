import { ToggleReposDirective } from './toggle-repos.directive';

describe('ToggleReposDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleReposDirective();
    expect(directive).toBeTruthy();
  });
});
