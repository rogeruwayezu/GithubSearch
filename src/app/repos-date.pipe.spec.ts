import { ReposDatePipe } from './repos-date.pipe';

describe('ReposDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReposDatePipe();
    expect(pipe).toBeTruthy();
  });
});
